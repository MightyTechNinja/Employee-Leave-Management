{
    "name": "employee-leave-management",
    "version": "1.0.0",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "start": "npm run start --prefix client",
        "build": "npm config set legacy-peer-deps true && npm config set legacy-peer-deps true --prefix client && npm install && npm install --prefix client && npm run build --prefix client && tsc --build"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@types/express": "^4.17.21",
        "express": "^4.18.2",
        "path": "^0.12.7"
    }
}
